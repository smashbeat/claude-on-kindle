# Cloudflare Tunnel connector token (Zero Trust dashboard -> Networks -> Tunnels ->
# your tunnel). Read by cloudflared. KEEP SECRET — never commit the real value.
TUNNEL_TOKEN=

# App config (optional; defaults shown)
KT_SESSION=claude
KT_HOST=127.0.0.1
KT_PORT=8882
